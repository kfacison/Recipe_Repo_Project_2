const express = require('express');
const router = express.Router();
const passport = require('passport');
const ingredientsRouter = require("../controllers/ingredients")

//router.get('/', ingredientsRouter.index);

module.exports = router;
